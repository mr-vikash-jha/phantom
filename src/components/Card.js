import React from "react";
import Button from "./Button";

const Card = () => {
  return (
    <div>
      <Button color={"#7337ab"} text="Create a new wallet" />
    </div>
  );
};

export default Card;
